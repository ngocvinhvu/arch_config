#!/bin/sh
curl -s "wttr.in/hanoi?0?T" | sed -n 4p | tr -d '.-' | awk '{print $1}' > /tmp/weather && a=`cat /tmp/weather | awk '$1 > 0 {print substr($1,1,2)}'`; b=`cat /tmp/weather | awk '$1 > 0 {print substr($1,3,4)}'` 
echo ❄️ $a°C 🌞$b°C
